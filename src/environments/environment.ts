// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  apiUrl: 'https://api.rorisafe.com/motiv/api',
  apiHost: 'https://api.rorisafe.com/motiv',
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.

/**/
 // Esto va en el arhcivo enviroment.ts  
 API_URL_CONSTRUCC_PROV: 'https://apicons.ddns.net:8092/api'
 API_URL_CONSTRUCC_APP: 'https://apicons.ddns.net:8091/api'
 TOKEN_CONSTRUCC_APP: '/%-!?=T35sT._22<1|:'
 
